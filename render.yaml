databases:
  - name: mvp-postgres
    plan: free

services:
  - type: web
    name: mvp-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm ci --include=dev && npm run prisma:generate && npm run build
    startCommand: npx prisma migrate deploy && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: mvp-postgres
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: WHATSAPP_WEBHOOK_VERIFY_TOKEN
        sync: false
      - key: REMINDER_WORKER_ENABLED
        value: "true"
      - key: INTERNAL_DISPATCH_SECRET
        generateValue: true
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_APPROVER_CHAT_ID
        sync: false
      - key: EZR_STAGING_URL
        sync: false
      - key: EZR_PHONE_NUMBER_ID
        sync: false
      - key: EZR_WABA_ID
        sync: false
      - key: EZR_TEST_PHONE
        sync: false
      - key: EZR_DEPLOY_FE_TARGET
        value: cloudflare
      - key: EZR_DEPLOY_BE_TARGET
        value: render
      - key: EZR_ORCHESTRATOR_WORKER_POLL_MS
        value: "15000"

  - type: worker
    name: mvp-orchestrator-worker
    env: node
    plan: starter
    rootDir: backend
    buildCommand: npm ci --include=dev && npm run build && npm install --no-save @openai/codex@0.116.0
    startCommand: npm run worker:orchestrator:api
    envVars:
      - key: NODE_ENV
        value: production
      - key: EZR_ORCHESTRATOR_API_BASE
        sync: false
      - key: EZR_ORCHESTRATOR_WORKER_ID
        value: render-worker
      - key: EZR_ORCHESTRATOR_WORKER_POLL_MS
        value: "15000"
      - key: EZR_AGENT_HEARTBEAT_INTERVAL_SECONDS
        value: "45"
      - key: EZR_AGENT_LEASE_TTL_SECONDS
        value: "90"
      - key: OPENAI_API_KEY
        sync: false
