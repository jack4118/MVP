Implemented the EzReply frontend plan:

- Unified `/ai` and `/leads` quick AI around a shared config model, option lists, and request builder.
- Reworked the leads quick AI modal into a compact synced draft experience with advanced options.
- Added EzReply logo branding, `/pricing`, and `/agent` pages.
- Updated upgrade modal and locale copy for Starter / Pro / Business presentation.
- Verified with `frontend` production build: `npm run build` passed.
